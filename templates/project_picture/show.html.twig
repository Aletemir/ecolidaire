{% extends 'base.html.twig' %}

{% block title %}ProjectPicture{% endblock %}

{% block body %}
    <h1>ProjectPicture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_picture.id }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ project_picture.filename }}</td>
            </tr>
            <tr>
                <th>Alt</th>
                <td>{{ project_picture.alt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('project_picture_index') }}">back to list</a>

    <a href="{{ path('project_picture_edit', {'id': project_picture.id}) }}">edit</a>

    {{ include('project_picture/_delete_form.html.twig') }}
{% endblock %}
